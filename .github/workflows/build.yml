# This is a basic workflow to help you get started with Actions
name: "Build OnlineShop"

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches:
      - master
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
  # This will build the project at 1am every Monday
  schedule:
    - cron: "0 0 * * 1"

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  #################################################################################################################################
#  build:
#    # The type of runner that the job will run on
#    runs-on: windows-latest
#
#    # Steps represent a sequence of tasks that will be executed as part of the job
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v1
#      
#      - name: Setup MSBuild Path
#        uses: microsoft/setup-msbuild@v1.0.2
#        
#      - name: Use Node.js
#        uses: actions/setup-node@v1
#        
#      - name: Setup NuGet.exe for use with actions
#        uses: NuGet/setup-nuget@v1.0.5
#     
#      - name: Restore NuGet Packages
#        run: nuget restore MVC_OnlineShop.sln
#
#      - name: Build and Publish Web App
#        run: msbuild MVC_OnlineShop.sln /p:Configuration=Release /p:DeployOnBuild=true /p:PublishProfile=Release
#
#      - name: Upload Artifact
#        uses: actions/upload-artifact@v1.0.0
#        with:
#          name: published_webapp
#          path: bin\Release\Publish
#################################################################################################################################
  #build-dotnet:
  #  name: Build dotnet
  #  runs-on: windows-latest
  #  steps:
  #    - name: Checkout
  #      uses: actions/checkout@v2
  #    - name: Build the Docker image
  #      run: docker build . --file Dockerfile --tag auycro/dotnet
  #      #run: docker build . --file Dockerfile --tag auycro/dotnet:$(date +%s)
  #    - name: Run the Docker image
  #      run: docker run --rm auycro/dotnet --version
#################################################################################################################################
#  build-msbuild:
#    name: Build msbuild
#    runs-on: windows-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v2
#      - name: Build the Docker image
#        run: docker build --file Dockerfile.msbuild --tag auycro/msbuild .
#        #run: docker build . --file Dockerfile --tag auycro/dotnet:$(date +%s)
#      - name: Run the Docker image
#        run: docker run --rm auycro/msbuild --version
#################################################################################################################################
  build: 
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Setup .NEt Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 2.2.108
      - name: Build with dotnet
        run: dotnet build --configuration Release
      - name: Test it
        run: dotnet test --verbosity normal
          
